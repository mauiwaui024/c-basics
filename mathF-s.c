// #include <stdio.h>
// #include <math.h>

// int main()
// {
//     double A = sqrt(9); //найти квадрат числа
//     double B = pow(2,4); //возвести число в степень
//     int C = round(3.14); //округляет 
//     int D = ceil(3.14); //округляет вверх, результат будет 4
//     int E = floor(3.99); //округляет вниз, результат будет 3
//     double F = fabs(-100); //модуль числа или absolute value, берет негативное число и делает положительным
//     double G = log(25); // ищет степень, в которую нужно возвести 2.71828, чтобы получилось 25
//     double H = sin(45);
//     double I = cos(45);
//     double J = tan(45);
//     printf("\n%lf", G);
//     return 0;
// }


// In mathematics, the natural logarithm (also known as the base-e logarithm or simply the logarithm) is the logarithm to the base of the mathematical constant e, where e is an irrational and transcendental number approximately equal to 2.71828. The natural logarithm of a number is the power to which e must be raised in order to equal that number. The natural logarithm is denoted as ln(x) or log(x) with base e.